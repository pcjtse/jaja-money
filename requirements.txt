streamlit
finnhub-python
python-dotenv
plotly
pandas
anthropic
transformers
torch
pytest
requests
# P1.3 PDF export (optional — gracefully skipped if absent)
reportlab
kaleido
# P1.5 disk cache / config
pyyaml
# P2.5 background alert polling (optional — gracefully skipped if absent)
APScheduler
plyer
# P3.5 multi-source fallback (optional — gracefully skipped if absent)
yfinance
# P4.4 test coverage
pytest-cov
# Dev tooling
ruff
# P10.5 PDF text extraction (optional — install one or both)
pdfplumber
# PyMuPDF (fitz) alternative: pip install pymupdf
# P12.2 Google Sheets export (optional)
gspread
google-auth
# P14.2 Redis cache backend (optional)
redis
# P14.3 FastAPI server mode (optional)
fastapi
uvicorn[standard]
httpx
# OpenClaw integration: Alpaca broker (optional — set ALPACA_API_KEY / ALPACA_API_SECRET)
